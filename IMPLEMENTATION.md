# MVP 实现清单

- [x] 响应式电脑/手机看板原型
- [x] MT5 只读采集接口（M5/M15/M30/H1、报价、点差）
- [x] 预测冻结、到期结算、方向/目标区间/触达/Brier 评分
- [x] 20/60 次滚动统计与 20 样本校准门槛
- [x] 固定分析 Skill 与字段协议
- [x] 计算 MACD、Stoch、ADX、MA 的基础实现与 200 根K线检查
- [x] 接入 GDELT、Federal Reserve RSS、BLS 官方日历并保存首次可见时间
- [ ] 接入模型并强制结构化输出；低质量输入直接空仓
- [x] 单次闭环入口：先结算、再采集、预测、导出和推送
- [x] Windows 任务计划已注册：10:00、16:00、21:00（邮件与飞书推送已启用）
- [x] 邮件 SMTP 与飞书机器人双通道适配器，独立返回失败原因
- [x] 网页读取真实预测、有效期、概率、目标区间和滚动成绩
- [x] 电脑端“立即分析”连接本机只读控制口
- [x] GitHub 公开仓库、Pages 部署与手机访问
- [x] GitHub Pages 自动构建部署流程
- [ ] 端到端试运行至少 5 个时段后再启用正式统计
- [x] 本机 MT5 首次真实只读联调通过

首版不含自动下单、自动调参、回测后挑选最佳组合。累计 100 个样本前冻结评分口径；后续再增加“指标组合版本”维度做样本外比较。

## 已确认的报告调整（待下一轮实现）

- 所有对外时间统一显示为北京时间，格式使用“2026-09-10 15:04 至 21:00（约 5 小时 56 分钟）”，不再直接展示 UTC ISO 字符串。
- 手动生成的报告也以“下一次固定更新时间”为有效期终点，不再简单增加 6 小时。
- 报告暂不提供精确入场价，也不把单个 MA 数值写成必须执行的价格。
- “执行条件”改为“建议操作”，输出观察、偏多、偏空或空仓，并说明适用场景。
- 使用 M5、M15、M30、H1 的摆动高低点、MA 聚集、前日高低收盘和波动尺度，合并成支撑区与阻力区；输出区间而非单点。
- K线形态继续只作为支撑/阻力附近的确认信息，不单独触发交易，也不重复增加指标权重。
- 区分三个概念：支撑/阻力区用于决策，建议操作用于展示，目标价区间用于预测结算。
- 原有统计保持不变：方向是否命中、到期价格是否落入目标区间、期间是否触达目标区间、Brier 分数和置信度校准。
- 在累计足够样本前冻结结算规则；支撑/阻力区暂不纳入胜负判定，避免修改历史统计口径。

## 最终建议输出格式（已确认）

- 分析内容保持不变：M5/M15/M30/H1、MA、MACD、Stoch、ADX/DI、新闻、日内行情、支撑阻力和K线确认。
- `方向`只允许：突破做多、突破做空、回踩做多、反弹做空、空仓等待。
- `理想入场`必须是价格区间，不输出单一精确点位。
- `核心区间`列出本方案依据的关键支撑区或阻力区，并说明其性质。
- `入场条件`固定要求等待 M5 K线收盘确认；未收盘、只有影线瞬时穿越或没有形态时，不视为触发。
- M5确认形态首版只使用：突破收盘确认、突破后回踩、看涨/看跌吞没、长下影/长上影拒绝。
- `止损区间`表示方案失效区域；价格有效进入该区间即放弃原方向，不表述为保证成交的精确止损价。
- `TP1/TP2`均输出区间：TP1采用最近一档结构目标，TP2采用下一档结构目标；若空间不足则直接空仓。
- `禁止事项`至少包含：禁止追涨追跌、禁止未等M5收盘确认、禁止临近高影响新闻入场、禁止点差异常时操作、禁止目标空间不足时强行交易。
- 突破与回踩方案不可同时作为主方案；系统只选一个主方案，另一方案只能作为备选或不输出。
- 最终操作由用户自行决定，系统不自动下单。
- 原有结算口径保持不变：方向、目标区间、触达、Brier和概率校准；入场、止损、TP1/TP2暂不参与历史胜负统计。
